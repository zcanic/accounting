# 测试问题报告 | Test Issues Report

**生成时间**：2025-12-27 11:30
**测试框架**：Vitest 4.0 + React Testing Library 16.3
**测试结果**：73 通过 / 21 失败 (77.7% 通过率)

---

## 📊 测试结果总览

### 通过测试文件
- ✅ **utils.test.js** - 全部通过 (60+ 用例)
- ✅ **webhook.test.js** - 全部通过 (30+ 用例)

### 失败测试文件
- ❌ **App.test.jsx** - 多个失败
- ❌ **useQuestionQueue.test.js** - 1 个失败

---

## 🐛 失败测试详细分析

### 问题 1：App.test.jsx 无法运行

**错误类型**：模块导入错误

**根本原因**：
```
TypeError: vi.mock is not a function
```

**详细分析**：
App.test.jsx 中使用了 vi.mock() 来模拟 useQuestionQueue hook，但由于文件后缀是 `.jsx`，Vitest 可能没有正确解析 vi 对象。

**影响范围**：整个 App.test.jsx 文件的所有测试用例（约 20 个）

**修复方案**：
1. **方案 A（推荐）**：将 mock 移到文件外部
   ```javascript
   // 在 src/test/mocks/hooks.js
   import { vi } from 'vitest';
   export const mockUseQuestionQueue = vi.fn();
   ```

2. **方案 B**：将文件重命名为 `.test.js`
   - 缺点：失去 JSX 语法支持

3. **方案 C**：使用 render 时的 wrapper 注入
   ```javascript
   const wrapper = ({ children }) => (
     <QuestionQueueProvider value={mockValue}>
       {children}
     </QuestionQueueProvider>
   );
   ```

**优先级**：🔴 高 - 阻止了所有 App 组件测试

---

### 问题 2：useQuestionQueue.test.js - 去重测试失败

**失败用例**：
```
❌ 应该拒绝重复的题目（基于内容哈希）
```

**错误信息**：
```javascript
expect(received).toBe(expected) // Object.is equality

Expected: 5
Received: 6  // 期望题目总数不变，但实际增加了
```

**根本原因**：
测试 mock 的题目 ID 是 'different-id'，但文本内容相同。由于 generateQuestionKey 优先使用 id 字段，不同的 ID 会被视为不同的题目，即使内容相同。

**相关代码**：
```javascript
// useQuestionQueue.js:15-18
const generateQuestionKey = (q = {}) => {
  if (q?.id) return `id-${q.id}`;  // ⚠️ 优先使用 id
  if (q?._id) return `_id-${q._id}`;

  // 只有在没有 id 的情况下才使用内容哈希
  const text = (q.text || q.scenario || '').trim().toLowerCase();
  // ...
};
```

**测试用例问题**：
```javascript
// useQuestionQueue.test.js:117-127
it('应该拒绝重复的题目（基于内容哈希）', async () => {
  global.fetch = vi.fn(() =>
    Promise.resolve({
      ok: true,
      json: () => Promise.resolve({
        scenarios: [
          {
            id: 'different-id',  // ❌ 不同的 ID
            text: '本地题目1',   // 相同的文本
            debit: ['科目A'],
            credit: ['科目B'],
            distractors: ['科目X']
          }
        ]
      })
    })
  );
  // ...
});
```

**修复方案**：

**方案 A（修改测试用例）**：移除 id 字段，强制使用内容哈希
```javascript
it('应该拒绝重复的题目（基于内容哈希）', async () => {
  global.fetch = vi.fn(() =>
    Promise.resolve({
      ok: true,
      json: () => Promise.resolve({
        scenarios: [
          {
            // ✅ 移除 id 字段
            text: '本地题目1',   // 相同的文本
            debit: ['科目A'],
            credit: ['科目B'],
            distractors: ['科目X']
          }
        ]
      })
    })
  );
  // ...
});
```

**方案 B（修改代码逻辑）**：优先使用内容哈希，仅在内容哈希冲突时才使用 id
```javascript
const generateQuestionKey = (q = {}) => {
  // 先生成内容哈希
  const text = (q.text || q.scenario || '').trim().toLowerCase();
  const debit = Array.isArray(q.debit) ? q.debit.sort().join('|') : '';
  const credit = Array.isArray(q.credit) ? q.credit.sort().join('|') : '';

  if (text || debit || credit) {
    // 有内容则使用内容哈希
    const contentHash = `${text}|${debit}|${credit}`;
    const simpleHash = contentHash.split('').reduce((acc, char) => {
      return ((acc << 5) - acc) + char.charCodeAt(0);
    }, 0);
    return `content-${(simpleHash >>> 0).toString(36)}`;
  }

  // 只有在完全没有内容时才使用 id
  if (q?.id) return `id-${q.id}`;
  if (q?._id) return `_id-${q._id}`;

  return 'content-0';
};
```

**推荐方案**：方案 A（修改测试用例）

**原因**：
1. 当前代码逻辑是合理的：有 id 时优先使用 id，性能更好
2. 测试用例设计有误：不应该在有 id 的情况下期望基于内容去重
3. 真实场景：webhook 返回的题目通常有 id

**优先级**：🟡 中 - 测试用例设计问题，不影响实际功能

---

### 问题 3：React act() warnings

**警告信息**：
```
Warning: An update to TestComponent inside a test was not wrapped in act(...)
```

**出现位置**：
- useQuestionQueue.test.js 中的多个测试用例
- 特别是涉及到 nextQuestion() 的测试

**根本原因**：
React 状态更新没有包裹在 act() 中，导致测试中的异步状态更新不同步。

**影响**：
- 警告不会导致测试失败
- 但可能导致测试不稳定（偶尔失败）

**修复方案**：
```javascript
// ❌ 错误写法
act(() => {
  result.current.nextQuestion();
  result.current.nextQuestion();
  result.current.nextQuestion();
});

// ✅ 正确写法
await act(async () => {
  result.current.nextQuestion();
});
await act(async () => {
  result.current.nextQuestion();
});
await act(async () => {
  result.current.nextQuestion();
});

// 或者使用 waitFor
await waitFor(() => {
  expect(result.current.currentIndex).toBe(3);
});
```

**优先级**：🟢 低 - 仅警告，不影响测试结果

---

## 📋 修复优先级列表

### 🔴 高优先级（立即修复）
1. **App.test.jsx 模块导入错误**
   - 影响：20+ 个测试用例无法运行
   - 预计时间：10 分钟
   - 修复方法：重构 mock 结构

### 🟡 中优先级（本周修复）
2. **useQuestionQueue 去重测试失败**
   - 影响：1 个测试用例失败
   - 预计时间：5 分钟
   - 修复方法：移除测试数据的 id 字段

### 🟢 低优先级（有空再修）
3. **React act() warnings**
   - 影响：测试可能不稳定
   - 预计时间：30 分钟
   - 修复方法：包裹所有状态更新

---

## 🎯 测试改进建议

### 1. 测试隔离性
**当前问题**：测试之间共享全局 fetch mock

**建议**：每个测试用例独立配置 mock
```javascript
beforeEach(() => {
  vi.clearAllMocks();
  // 为每个测试设置默认 mock
  global.fetch = vi.fn(() => defaultSuccessResponse);
});
```

### 2. 测试数据管理
**当前问题**：测试数据散落在各个测试用例中

**建议**：创建测试数据工厂
```javascript
// src/test/fixtures/questions.js
export const createMockQuestion = (overrides = {}) => ({
  id: 'test-001',
  text: '测试题目',
  debit: ['借方科目'],
  credit: ['贷方科目'],
  distractors: ['干扰项'],
  ...overrides
});
```

### 3. 异步测试稳定性
**当前问题**：大量使用 setTimeout 和固定延迟

**建议**：
- 使用 `waitFor` 替代 `setTimeout`
- 增加超时时间配置
- 使用 `fake timers` 控制时间

### 4. Mock 策略优化
**当前问题**：vi.mock 与 JSX 不兼容

**建议**：
- 使用 dependency injection
- 创建专门的 test-utils
- 使用 React Context 注入依赖

---

## 📊 修复后预期结果

修复以上问题后，预期测试结果：

| 指标 | 当前 | 修复后预期 |
|------|------|-----------|
| 通过率 | 77.7% | 95%+ |
| 失败用例 | 21 | ≤5 |
| 警告数 | 多个 | 0 |
| 覆盖率 | 未测量 | 85%+ |

---

## 🔧 快速修复指南

### 修复问题 1：App.test.jsx

```bash
# 步骤 1：创建 mock 文件
mkdir -p src/test/mocks
touch src/test/mocks/useQuestionQueue.js
```

```javascript
// src/test/mocks/useQuestionQueue.js
export const createMockUseQuestionQueue = (overrides = {}) => ({
  currentQuestion: {
    id: 'test-001',
    text: '企业用银行存款购买原材料入库。',
    debit: ['原材料'],
    credit: ['银行存款'],
    distractors: ['应付账款', '库存商品', '预付账款']
  },
  currentIndex: 0,
  totalQuestions: 10,
  isLoading: false,
  isFetchingMore: false,
  isAwaitingNext: false,
  hasCheckedRemote: true,
  hasRemoteData: true,
  error: null,
  nextQuestion: vi.fn(),
  getAccountOptions: () => [
    '原材料',
    '银行存款',
    '应付账款',
    '库存商品',
    '预付账款'
  ],
  ...overrides
});
```

### 修复问题 2：去重测试

```javascript
// useQuestionQueue.test.js:117-127
it('应该拒绝重复的题目（基于内容哈希）', async () => {
  global.fetch = vi.fn(() =>
    Promise.resolve({
      ok: true,
      json: () => Promise.resolve({
        scenarios: [
          {
            // 移除 id 字段，强制使用内容哈希
            text: '本地题目1',
            debit: ['科目A'],
            credit: ['科目B'],
            distractors: ['科目X']
          }
        ]
      })
    })
  );
  // 测试逻辑不变...
});
```

---

## 📝 总结

### ✅ 做得好的地方
1. **工具函数测试**：utils.test.js 全部通过，覆盖全面
2. **Webhook 验证**：webhook.test.js 覆盖了各种边界情况
3. **测试用例设计**：大部分测试用例逻辑清晰，易于理解

### ❌ 需要改进的地方
1. **Mock 策略**：与 JSX 不兼容导致大量测试失败
2. **测试数据设计**：去重测试用例设计不合理
3. **异步处理**：React act() 警告需要修复

### 🎯 下一步行动
1. 修复 App.test.jsx 的 mock 问题
2. 调整去重测试用例的测试数据
3. 增加测试覆盖率报告
4. 集成到 CI/CD 流程

---

**报告生成者**：Claude Code Test System
**测试负责人**：测试自动化团队
**审核状态**：待审核
